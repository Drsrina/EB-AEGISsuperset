=== PRÉ-REQUISITO (JÁ EXECUTADO) ===
Certifique-se de que o repositório existe (você já rodou isso):
gcloud artifacts repositories create superset-repo --repository-format=docker --location=us-central1 --description="Superset Repo"

=== CONFIGURAÇÃO MANUAL NO GOOGLE CLOUD RUN ===

Como não estamos rodando scripts locais, você deve configurar as variáveis de ambiente manualmente após o primeiro deploy (que falhará por falta de senha, mas o serviço será criado).

1. Acesse o Google Cloud Console -> Cloud Run.
2. Clique no serviço 'superset-prod' (assim que ele aparecer na lista).
3. Clique em 'EDITAR E IMPLEMENTAR NOVA REVISÃO' (Edit & Deploy New Revision).
4. Vá na aba 'VARIÁVEIS E SEGREDOS' (Variables & Secrets).
5. Adicione as seguintes variáveis:

   NOME: SQLALCHEMY_DATABASE_URI
   VALOR: postgresql://postgres.inrknjgvnfzumujlbfad:lzYjxisge2UpyOFG@aws-0-us-west-2.pooler.supabase.com:5432/postgres

   NOME: SECRET_KEY
   VALOR: (Digite uma senha longa e aleatória aqui)

   NOME: GUNICORN_CMD_ARGS
   VALOR: --timeout 120 --workers 2 --threads 4 --worker-class gthread

6. Clique em IMPLEMENTAR (Deploy).
